# BPNet Multi-Task Interaction Predictor

基于 **BPNet 膨胀卷积架构**的多任务基因组相互作用预测模型，用于 **EPI & EEI** 预测。

## 环境要求

* Python 3.8+
* PyTorch >= 1.10.0 (CUDA)
* NumPy
* scikit-learn
* Matplotlib

```bash
pip install torch numpy scikit-learn matplotlib
```

## 数据格式

输入数据为 `.npz` 文件，包含：

* `sequence`：DNA 序列 one-hot 编码，可重塑为 `4 × 5000`
* `label`：交互标签（0/1）

## 快速运行

在 `main.py` / `train.py` 中配置参数：

```python
model_params = {
    'filters': 64,
    'n_dil_layers': 4,
    'counts_loss_weight': 0.1,
    'inputlen': 5000,
    'outputlen': 1000
}
```

启动训练与评估：

```bash
python main.py
```

模型完成训练后将自动进行测试评估，并生成 **ROC/PRC** 曲线。
